<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spike.cloud.dao.SecKillOrderDao">

    <insert id="addOrder" parameterType="com.spike.cloud.entities.SecKillOrder" >
        insert into secKill_order(orderId, customerId, movieId, saleDate) values (#{orderId}, #{customerId}, #{movieId}, #{saleDate});
    </insert>

</mapper>